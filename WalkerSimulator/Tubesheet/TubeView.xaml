<UserControl x:Class="WalkerSimulator.tubesheet.TubeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WalkerSimulator.tubesheet"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid ToolTip="{Binding ToolTipTxt}" Width="{Binding Pitch}" Height="{Binding Pitch}">
        <!-- Draws an oval with a blue interior. -->
        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="{Binding TubeColorCode}"/>
    </Grid>
</UserControl>
